import { Router } from 'express';

const router = Router();

// Internal routes will be added here as features are implemented
// Example: router.use('/calculation', calculationRoutes);

export default router;
